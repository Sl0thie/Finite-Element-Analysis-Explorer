<Page
    x:Class="Finite_Element_Analysis_Explorer.Details"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Finite_Element_Analysis_Explorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Width="320">

    <ScrollViewer Width="380" HorizontalAlignment="Left">
        <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Button x:Name="SaveButton" Background="Transparent" Foreground="White" Padding="0" BorderThickness="0" Margin="6,6,2,6">
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="New"/>
                        <MenuFlyoutItem Text="Open"/>
                        <MenuFlyoutItem Text="Save"/>
                        <MenuFlyoutItem Text="SaveAs"/>
                        <MenuFlyoutItem Text="Exit"/>
                    </MenuFlyout>
                </Button.Flyout>
                <SymbolIcon Symbol="Save" Foreground="White"/>
            </Button>
        </StackPanel>
    </ScrollViewer>
</Page>
